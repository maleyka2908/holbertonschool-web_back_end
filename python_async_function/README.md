# Python - Async Function

This project introduces asynchronous programming in Python using `asyncio`. It covers creating coroutines, waiting for multiple tasks, and understanding the event loop.

## Tasks
- `0-basic_async_syntax.py`: An asynchronous coroutine that waits for a random delay and returns it.
